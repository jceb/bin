#!/usr/bin/env python2
# -*- coding: utf-8 -*-
#
# Display date in dwm's status line

from Xlib.display import Display
from datetime import datetime
import time

r = Display().screen().root

while True:
	r.set_wm_name(datetime.now().strftime(u'%a %b %e %T %Y'))
	r.get_wm_name()
	time.sleep(1)
